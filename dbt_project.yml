name: 'learnwordstha'
version: '1.0.0'
profile: 'postgres_localhost'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  learnwordstha:
    +materialized: view

data_tests:
  +store_failures: true
  +database: LearnWords
  +schema: DBT_TEST__AUDIT